# Core LangChain packages
langchain>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.2.0

# Vector Database
chromadb>=0.4.24

# PDF Processing
pypdf
cryptography>=3.1  # ← ADD THIS (needed for encrypted PDFs)

# Environment Variables
python-dotenv

# OpenAI API
openai>=1.0.0  # ← ADD THIS (explicit OpenAI dependency)

# Streamlit UI
streamlit>=1.28.0  # ← ADD THIS (for the UI)

# Optional but recommended
tiktoken  # ← ADD THIS (for token counting, used by OpenAI)